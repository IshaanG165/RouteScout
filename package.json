{
  "name": "transitflow-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && python3.13 -m uvicorn app.main:app --reload --port 8000",
    "ingest": "cd backend && python3.13 scripts/ingest_gtfs.py"
  }
}
